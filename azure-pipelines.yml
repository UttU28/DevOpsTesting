trigger:
  - none  # Replace with your branch name if needed

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: |
    git clone https://github.com/stacksimplify/azure-devops-aks-kubernetes-terraform-pipeline.git
  displayName: 'Clone repository'

- script: |
    exit 0  # Replace with appropriate exit code if needed
  displayName: 'Exit'
